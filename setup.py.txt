from setuptools import setup, find_packages

setup(
    name='terrance',
    version='1beta',
    packages=find_packages(),
    install_requires=[],
    entry_points={
        'console_scripts': [
            'terrance = terrance.roboctl:main',
        ],
    },
)